To generate a self-signed developer certificate, run:

    sudo ./localhost_cert.sh

When asked for FQDN, enter localhost. Other questions are optional to answer.

    Common Name (e.g. server FQDN or YOUR name): localhost